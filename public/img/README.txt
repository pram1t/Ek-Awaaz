Drop the portrait here as smiti-didi.jpg (or .png and update the src in public/index.html).
Square crop works best; the CSS circles it and focuses at 50% 22%.
If the file is absent the page shows a lettered circle instead, so it never looks broken.
